//setInterval(cal,time) repeatedly calls a function or executes a code 	snippet, with a fixed time delay between each call.
let count = 0
setInterval(() => {count++; console.log("This Is SetInterVal", count)}
, 3000)
